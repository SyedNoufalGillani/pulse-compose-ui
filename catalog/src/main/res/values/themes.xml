<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Compose owns all theming at runtime (see PulseTheme). This XML theme exists only to
        satisfy the platform's window/launch requirements (splash background, status bar)
        before the first Compose frame is drawn.
    -->
    <style name="Theme.PulseCatalog" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
