# Consumer ProGuard rules applied to modules that depend on :pulseui.
# Compose compiler stability annotations are preserved automatically by the R8 Compose rules
# shipped with the Android Gradle Plugin, so no additional keep rules are required here.
